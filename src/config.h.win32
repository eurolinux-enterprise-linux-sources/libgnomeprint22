/* handcrafted hack to build with msvc on win32 */
#define HAVE_GDI 1
#define VERSION "2.11.0"
#define GETTEXT_PACKAGE "gnome-print"
#define M_SQRT1_2 (1.0/M_SQRT2)

#define bzero(p,n) memset(p,0,n)

#define popen(f,m) _popen(f,m)
#define pclose(f) _pclose(f)
